# Example environment variables
# Do NOT commit real secrets to the repository. Create a local .env with real values if needed.
OPENAI_API_KEY=your-openai-api-key-here
OPENAI_MODEL=gpt-4o
VODER_AGENTIC_MODE=false
